[project]
name = "mcpbook-web"
version = "1.0"
description = "MCPBook web backend"
requires-python = ">=3.10"
dependencies = [
    "flask>=3.0",
]

[tool.setuptools.package-data]
"web.resources" = ["*.txt"]
